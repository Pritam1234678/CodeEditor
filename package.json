{
  "name": "pritam-compiler",
  "version": "1.0.0",
  "main": "api.js",
  "scripts": {
    "start": "node api.js",
    "build": "echo 'Skipping system package install in Render'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "body-parser": "^1.20.3",
    "compilex": "^0.7.4",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
